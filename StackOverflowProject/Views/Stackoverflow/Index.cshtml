
@model StackOverflowProject.Controllers.StackoverflowController.ViewModel


<h2 class="mb-3">Stack Overflow Questions</h2>



<ul class="list-group">

    @foreach (var question in Model.Questions)
    {
        <li class="list-group-item p-0">
            <div class="p-3 border-bottom bg-light">
                <div class="d-flex justify-content-between align-items-start gap-2">
                    <div>
                        <div class="fw-semibold">@question.Title</div>
                        <small class="text-muted">@question.AnswerCount answers</small>
                    </div>
                </div>
            </div>
            <ul class="list-group list-group-flush">
                @foreach(var answer in question.Answers)
                {
                    <li class="list-group-item ps-4 small">
                        @if (answer.AcceptedAnswer)
                        {
                            <span class="badge bg-success me-2">Accepted</span>
                        }
                        @answer.Body
                    </li>
                }
            </ul>
        </li>
    }
</ul>
